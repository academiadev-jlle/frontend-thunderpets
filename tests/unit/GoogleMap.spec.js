import Vue from 'vue';
import Vuetify from 'vuetify';

describe('GoogleMap.vue', () => {
  beforeEach(() => {
    Vue.use(Vuetify);
  });

  it('test for the sake of having one', () => {
    expect(true).toBeTruthy();
  });
});
